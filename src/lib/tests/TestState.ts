export interface TestState {
    current: {
        word: {
            index: number
            length: number
        }

        character: {
            index: number
            symbol: string
        }
    }
}